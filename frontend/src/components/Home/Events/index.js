const Events = ({ events }) => {
    return (
        <div className="homePageEventsContainer">
            <div className="homePageEvents">
                <ul className="homePageEventList">
                    <li className="homePageEventCard">Events Near You</li>
                </ul>
            </div>
        </div>
    );
};

export default Events;
